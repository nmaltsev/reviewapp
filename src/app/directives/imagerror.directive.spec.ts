import { ImagerrorDirective } from './imagerror.directive';

describe('ImagerrorDirective', () => {
  it('should create an instance', () => {
    const directive = new ImagerrorDirective();
    expect(directive).toBeTruthy();
  });
});
